# Runtime dependencies (core + web). OpenCV is optional — see requirements-training.txt
pyyaml>=6.0
pillow>=10.0
pyserial>=3.5
fastapi>=0.110
uvicorn[standard]>=0.27
python-jose[cryptography]>=3.3
bcrypt>=4.0
python-multipart>=0.0.9
aiofiles>=23.0
httpx>=0.27

# Pi only (apt, not pip): python3-picamera2 python3-lgpio
